scripts:
  build: |
    nps build:vite-plugin
  build:vite-plugin: |
    cd packages/vite-plugin-multiline-tailwind-css
    rm -rf dist
    tsup-node
  lint: |
    eslint \
      packages/vite-plugin-multiline-tailwind-css/src \
      --max-warnings 0 \
      --report-unused-disable-directives
  test: jest
