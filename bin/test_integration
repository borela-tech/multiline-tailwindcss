#!/usr/bin/env -S bash -e -o pipefail

SCRIPT_DIR=$(dirname -- "${BASH_SOURCE[0]}")
pushd "$SCRIPT_DIR/.." > /dev/null

npx jest --verbose --testMatch="**/integration/**/*.test.ts?(x)"

popd > /dev/null
